---
-hosts : Webserver
 become : true
 tasks:
   -name : update cache and make sure vim, curl, unzip are installed
   apt : 
      name : "{{item}}"
      update_cache : yes
    loop :
      -vim
      -curl
      -unzip